machine:
  kubelet:
    extraMounts:
      - destination: /var/openebs/local
        source: /var/openebs/local
        type: bind
        options:
          - rbind
          - rshared
          - rw
